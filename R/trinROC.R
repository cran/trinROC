#' @details See \code{vignette("Overview", package = "trinROC")} for an overview of the package.
#'   Further, sd(), var() and cov() are chosen with \code{options(trinroc.MLE = TRUE)} according to the maximum likelihood estimates (\code{default}). Change to sample
#'   estimates by setting \code{options(trinroc.MLE = FALSE)}
#' @references Noll, S., Furrer, R., Reiser, B. and Nakas, C. T. (2018).
#'   Inference in ROC surface analysis via a trinormal model-based testing approach.
#'   \emph{Submitted}.
"_PACKAGE"
